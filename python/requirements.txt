# core: needed to boot DataOS + engine + evals
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.1
httpx>=0.27.0
rich>=13.7.1
numpy>=1.26.0
pandas>=2.0.0
networkx>=3.0
scipy>=1.10.0
scikit-learn>=1.3.0
# needed by security/gen-local-ca.py, which the tls_handshake eval invokes
cryptography>=42.0.0

# optional: TRUE semantic memory recall. Without it, memory falls back to
# hashed word vectors (zero deps) which match lexical variants but NOT
# paraphrases ("car" will not find "Ford Focus").
# Cost: pulls torch (~2GB) + ~90MB model, and a ~60s first-load.
# pip install sentence-transformers
# Verify what you got:  python agent-os/python/memory_recall_report.py

# optional: lazy-imported inside functions only (import_hygiene eval enforces).
# install only what you use:
# boto3                      # S3Storage (data-kernel/infrastructure/storage/cloud_store.py)
# google-cloud-storage       # GCSStorage (same file)
# azure-storage-blob         # AzureBlobStorage (same file)
# sentence-transformers      # neural embeddings (vector_index.py, embeddings_api.py)
# psycopg2-binary            # PostgresStorage + DB connectors
# reportlab                  # loom PDF export (engine/tools/pdf.py)
